# Sub-20 cm Campaign — Baseline Measurements (2026-08-17)

Harness: `scripts/eval_ball_accuracy.py` (spec §4). Hold-out = stratified
alternating 50 % of manual anchors withheld, graded at the withheld frames on
the emitted dense track; both folds run, every anchor held out exactly once.
Errors are 3-D metres where GT exists (`ground_exact` via ray ∩ z=r,
`joint_depth` via contacting joint on the clicked ray − one ball radius,
matching the resolver's convention), else lateral ray distance (a lower
bound). Threshold 0.20 m. Raw reports: `runs/baseline_*.json`.

## A1 hold-out, no-op detector (anchor-driven path)

Current default config (`solver: events`, auto anchors on):

| clip | n held out | p50 | p95 | max | >20 cm |
|---|---|---|---|---|---|
| gberch | 55 | **0.44 m** | 2.44 | 8.40 | 41/55 |
| origi01 | 57 | **0.90 m** | 6.04 | 24.23 | 47/57 |
| kroupi01 | 12 | **2.32 m** | 6.94 | 7.77 | 12/12 |
| s013 | 13 | **4.90 m** | 12.69 | 12.76 | 12/13 |

Kept (non-withheld) anchors grade 0.000 m everywhere — the operator-wins
pinning works; **all error lives between/without anchors**, exactly where the
goal's "any given time" bites. Naturalness: 0 violations on all clips — the
events renderer is structurally smooth; accuracy, not smoothness, is the
failure mode. (Caveat: the validator trusts event frames, so phantom auto
events legitimise their own bends — see finding 2.)

Per-state (gberch / origi01, held out): grounded p50 0.35 / 1.18; touches
p50 0.85 / 2.74 (all touches > 20 cm on both); airborne_mid/high all > 20 cm.

## Decomposition (gberch / origi01, hold-out p50 | max)

| variant | gberch | origi01 |
|---|---|---|
| default (auto events on) | 0.44 / 8.40 | 0.90 / 24.23 |
| kinematic_touch off | 0.47 / 8.40 | 0.97 / 24.23 |
| pure interpolation (all auto minting off) | 0.47 / 6.00 | 0.90 / 24.23 |

## Findings — dominant error sources (ranked)

1. **Between-keyframe interpolation is decimetre-to-metre wrong** (the bulk
   of p50). Rolls/carries render constant-speed linear; ballistic spans are
   two-endpoint parabolas through bucket-height endpoints. A held-out touch
   means the path misses the real contact by metres (gberch f371: 6.0 m;
   origi01 f282 airborne header: 24.2 m).
2. **Phantom auto touches inject hard keyframes metres off the path.**
   With zero real detections the stage still minted 24 auto anchors on
   gberch (fold 1), incl. `f170 player_touch P004 l_knee conf=0.94` — 8.4 m
   from GT, on a span where plain linear interpolation lands within ~5 cm.
   Mechanism: anchor pixels (conf 1.0) + appearance-bridge template matches +
   IMM gap-fill form a synthetic pixel track; direction-change segmentation
   finds corners in it; pose_touch attributes any corner to the nearest joint
   within 60 px (high-recall by design, "editor prunes"). Unattended, these
   become body-pinned keyframes that drag the track to the wrong player.
   The naturalness validator cannot flag them (the bend is at an "event").
3. **Airborne keyframes are height-bucket placeholders** (`ray_physics` at
   z = 1.0/6.0/15.0). origi01 airborne_mid/high held-out: 11/11 > 20 cm,
   p50 0.62–1.08 m. The ≥2-hard-knot gravity solve exists only in the unused
   piecewise path.
4. **Touch keyframes can resolve below the pitch** (gberch touches at
   z = −0.08…−0.01; s013 f42 at z = 0.006): FK foot-anchor residual passes
   straight into the ball position at the priority moments.
5. Sparse-anchor clips (kroupi01 12, s013 13 anchors) have long unconstrained
   spans → p50 2.3–4.9 m: they need the detection evidence (A3 path) and
   evidence-constrained rendering far more than anchor-dense clips.

## A2/A3 (real detector) — WASB fine-tuned v1, local MPS

gberch hold-out with `--detector wasb`: run in progress; numbers land in
`runs/w4_gberch_wasb_holdout.json` (this doc updates when recorded).
origi01 A2 vs 31 cross-replay fixes: queued behind it. Detector inference is
cached content-keyed under `det_cache/` (`--det-cache`), so pre-campaign
wasb numbers are reproducible cheaply from the baseline commit.

## W2/W3/W4 measured deltas (noop hold-out, p50 | max)

| clip | baseline | after W2 (clamp+gate) | after W3 (+chains, min-move clamp) |
|---|---|---|---|
| gberch | 0.44 / 8.40 | 0.35 / 6.00 | 0.45 / 6.00 |
| origi01 | 0.90 / 24.23 | 0.76 / 24.23 | 0.87 / 24.23 |
| kroupi01 | 2.32 / 7.77 | 3.27 / 7.77 | 3.27 / 7.77 |
| s013 | 4.90 / 12.76 | 5.85 / 13.13 | 5.85 / 12.77 |

Readings:
- The 8.4 m phantom-touch class is gone (gberch max = pure-interp level).
- kroupi01/s013 regress under the gate because on noop hold-out their only
  densification was synthetic-born events, whose helpful members deviate
  3.4–7.7 m from the sparse manual reference — statistically inseparable
  from the 8.6 m phantom (measured). Decision: keep the conservative gate;
  sparse clips' accuracy must come from real detection evidence (W4), which
  is also the operating point (s013 detection coverage 0.99).
- W3 chain refits rarely fire on noop hold-out (bracketing hard knots are
  themselves often held out); their value arrives with W4 detection
  evidence. Synthetic-arc unit tests prove the machinery (≤5 cm on truth).
- origi01 kept-touch 5.03 m regression from the first W2a clamp
  (along-ray slide on a shallow ray) was found by the harness's kept-anchor
  sanity check and fixed with the min-move clamp; kept anchors are back to
  ≤0.36 m (within joint-GT model uncertainty), 0.00 elsewhere.
- Naturalness violations remain 0 throughout.

W4 (evidence-constrained rendering: rolls follow detection ground points,
chain fits consume in-span detections, free-flight renders gravity arcs,
refit keyframes ray-snapped pre-render; W4b carry follows the owning
player's foot path) is inert on noop runs by design — its measurement is
the cached wasb runs.

## Operational A3/A2 on the SAVED (GPU-run, all-anchors) tracks

Dense lateral vs recorded hard-source detections (conf ≥ 0.5), naturalness,
and origi01 fixes — measured after fixing the A3 loader (the sidecar key is
`frames`, not `observations`; the "all sidecars empty" reading earlier in
the campaign was this bug — GPU-run evidence was there all along):

| clip | dense n | p50 | p95 | max | >20 cm | naturalness |
|---|---|---|---|---|---|---|
| gberch | 313 | 0.029 | 0.483 | 4.15 | 29 | 0 |
| origi01 | 347 | 0.047 | 0.313 | 2.43 | 27 | 0 |
| origi02 | 48 | 1.083 | 24.6 | 30.0 | 30 | 0 |
| kroupi01 | 43 | 0.260 | 2.12 | 6.23 | 28 | 0 |
| s013 | 196 | 0.038 | **0.197** | 0.49 | 10 | 0 |

Where detections exist and anchors are dense, lateral error is already
centimetres (s013 nearly meets A3 outright); the tails live where track and
evidence diverge (occlusion spans, low-coverage clips — origi02).

## W5 iteration log (gberch, wasb hold-out — real evidence)

| run | p50 | p95 | max | >20cm | violations | what changed |
|---|---|---|---|---|---|---|
| noop baseline | 0.44 | 2.44 | 8.40 | 41/55 | 0 | — |
| w4 (first wasb) | **0.173** | 5.52 | 8.62 | 27/57 | 3 | real evidence in |
| w5ab | 0.207 | 4.69 | 5.76 | 29 | 10 | chord gate kills the f170 false-cluster 8.6 m |
| w5c/d | 0.246 | 4.69 | 5.83 | 30 | 8 | soft auto knots, depth-aware attribution, smoothing |
| w5e | 0.271 | **3.25** | 5.83 | 31 | 8 | **anchor clicks exempt from IMM gate + reseed** |

Key W5 findings (each verified by probe, fix committed):
- **False pass-1 clusters** (detector locked on static objects post-kick,
  conf 0.6–0.8) steered evidence rolls 8 m — chord-deviation gate (1.5 m).
- **Junk in-span detections** poisoned chain fits (residuals 50–417 px) —
  two-stage robust fit (anchors-only floor, ≤30 px gated extras, median
  anchor-click acceptance).
- **Auto knots carry attribution error as 3-D error** — soft
  confidence-weighted world constraints (0.5 px/m tiebreak with a manual
  knot; 5 px/m when they're the only depth anchor; measured sweep).
- **The touch contact gate is depth-blind** (a joint near the camera–ball
  LINE passes while metres off along it) — depth-aware attribution scoring
  vs ground-anchor-interpolated expected worlds.
- **The IMM outlier gate rejected operator clicks** (post-kick ball beats
  the innovation gate), freezing the observation track at stale positions
  where static false detections then pass as consistent — anchor frames
  are now exempt and reseed the filter. This one fix moved p95 4.69→3.25
  and cleared the f55/56 cluster. It violated "operator input always wins"
  and had been silently corrupting the shared-core event stream.
- Auto touch **moments** are now found at relaxed recall 6/8 (0.75) vs the
  5/8 union reference; strict (bone-correct) 3/8 — attribution is the gap.
- Remaining tails: f188-class corner-cuts where the bend's event is held
  out AND its auto recovery is suppressed by a nearby kept manual anchor
  of a different state (suppress_radius experiment pending); residual
  naturalness violations are the validator correctly flagging real bends
  whose explaining event is missing from the keyframe set.

**A2 downgraded to a consistency indicator.** The origi01 cross-replay
fixes disagree with the saved track by p50 3.15 m — but a sensitivity
retriangulation shows the fixes themselves move p50 1.52 m (max 8.2 m)
between the saved (−142) and refined (−145.75) sync offsets; at ball speed,
sub-frame sync error is metres of GT motion. Cross-replay cannot gate a
20 cm target until pair sync is nailed to ≲0.5 frame (candidate W5+ work:
robust fine-grid offset refinement on hard-source obs; miss-vs-offset was
0.276 m at −142 vs 0.449 m at −145.75 on today's probe, contradicting the
recorded refinement — the estimator is fragile). Primary gates remain
A1 (hold-out, camera-relative), A3 (dense), A4/A5.

## Implications for the workstreams (spec §5–§8)

- W2 splits into **W2a touch contact geometry** (below-ground clamp — finding
  4) and **W2b evidence-gated auto events** (finding 2): touch proposals
  whose local pixel support is synthetic (anchor-interp/bridge/gap-fill, no
  detector hits nearby) must stay editor *suggestions* and never become
  unattended keyframes; body-pins that displace the ball far from the
  evidence-consistent path without detection support get demoted.
- W3 (airborne physics chains) directly addresses finding 3 (and the 24 m
  origi01 tail).
- W4 (evidence-constrained rendering incl. detection-following rolls and
  carry-follows-player) addresses findings 1 and 5.

## Commands

```bash
# per clip (output dir, shot):
.venv311/bin/python scripts/eval_ball_accuracy.py --output <dir> --shot <shot> \
    --holdout --detector noop --json runs/baseline_<shot>_noop_holdout.json
# decomposition variants: --set ball.kinematic_touch.enabled=false
#   [--set ball.auto_anchors.enabled=false --set ball.pose_touch.enabled=false]
# real detector: --detector wasb; A2: --fixes <ball_fixes.json>
```

## Cross-clip w5e (wasb hold-out) vs noop baseline

| clip | p50 | p95 | max | dense p50 | recall s/r | noop-baseline p50 |
|---|---|---|---|---|---|---|
| gberch | 0.27 | 3.25 | 5.83 | 0.034 | 3/6 of 8 | 0.44 |
| s013 | **0.52** | 3.73 | 6.19 | 0.042 | 2/4 of 8 | 4.90 (10× better) |
| origi01 | **0.41** | 4.99 | 24.2 | 0.050 | 3/7 of 15 | 0.90 |
| kroupi01 | **5.08** ⚠ | 9.39 | 10.9 | 0.746 ⚠ | 1/1 | 2.32 (regressed) |

Real evidence transforms the well-detected clips; kroupi01 (detector
holdout clip, coverage 0.53) regresses — weak/false detections feed the
evidence machinery. Its dense p50 of 0.75 m (track vs its own accepted
detections) marks a clip-level failure mode to probe: likely false
clusters + sparse-anchor chains. origi01 naturalness spiked to 16
(heading_break 14) — jitter-bends on long spans; f282 24 m header tail
persists (deep-flight chain or joint-depth GT weakness).

### kroupi01 regression — diagnosed (next lever)

Probe (fold 0, warm cache): 12 auto anchors minted, 11 of them touches,
including consecutive bursts (P004 @ 6/7/14/21, P005 @ 119/120/121) — the
clip is one aerial cross/shot sequence and these are players the ball flew
OVER: their joints sit near the ball's camera ray (the depth-blind contact
gate), real detections nearby satisfy the evidence gate, and the body-pins
render the flying ball along the ground (dense lateral 0.75 m median vs
its own detections; 5 m spans at f32/58–65/122–148). Fix direction: gate
auto TOUCH minting on the IMM flight state (p_flight high ⇒ only
header/chest candidates or a bounce/landing may mint), and suppress
consecutive-burst touches; the depth-aware attribution's expected worlds
must switch to the fitted flight arc (not ground interp) over flight
spans. This is the documented next work item.

## W5g calibrated stack — cross-clip closing table (this cycle)

wasb hold-out (p50 / p95 / max, metres) vs noop baseline p50:

| clip | w5g | baseline p50 | naturalness |
|---|---|---|---|
| gberch | 0.30 / 3.59 / 5.83 | 0.44 | 9 |
| origi01 | 0.45 / 4.45 / 24.2 | 0.90 | 17 ⚠ |
| s013 | 0.81 / 3.77 / 6.19 | 4.90 | 8 |
| kroupi01 | 2.52 / 9.06 / 10.9 | 2.32 | 4 |

Flight gate calibrated to p_flight 0.8 + adjacent-frame burst NMS: keeps
kroupi's phantom-fly-over rescue (5.08→2.52) while gberch's tail recovers
(p95 5.17→3.59). Per-clip sensitivity to the gate differs (s013 gave back
some of its w5e gain: 0.52→0.81); the calibration is a compromise surface.
Dense lateral p50 stays 3.3–5.7 cm on all clips.

Next levers, impact-ordered (each with a diagnosed mechanism):
1. **Bone-level attribution** (strict 3/8 vs relaxed 6/8 recall): wrong
   bone = wrong body-pin = decimetres at the priority moments. Needs
   two-pass attribution with fitted-arc expected depths over flight.
2. **origi01 naturalness spike** (15 heading_breaks): jitter bends from
   evidence-following on long spans — likely needs stronger smoothing or
   knot consensus on that clip's distant zoom.
3. **kroupi01 flight rendering** (dense 0.57 m): the aerial sequence needs
   chain fits to fire (currently underconstrained: 4 kept anchors) — the
   detection evidence exists but flight spans need arc fits from
   detections + goal geometry without interior anchors.
4. f188-class corner-cuts: bends whose only witness is the held-out
   anchor — the operator-information limit; honest reporting in place.

## W5h/W5i closing (2026-08-18)

- **W5h two-pass attribution** (context-bridged expected depths, re-mint,
  re-resolve once): gberch strict touch recall **3/8 → 4/8** with hold-out
  p50/p95 unchanged (0.297/3.59). The priority-moment metric improves.
- **W5i flight-probability segment reclassification**: implemented, tested,
  measured on kroupi01 (its target clip) — dense worsened 0.57→0.91 m at
  both 0.6 and 0.8 thresholds, so the resolver wiring was REVERTED
  (capability + unit tests retained). Lesson recorded: flipping a span to
  ballistic without fixing its endpoint keyframes renders a wrong arc;
  re-enable only with detection-driven flight-span arc fitting.
- Measurement-cost note: code changes that shift the accepted track move
  the second-pass/foot-guided crop positions, so each iteration re-pays a
  slice of crop inference despite the cache (~30–120 CPU-min per clip).

## Operational (full-anchor) gberch — as-shipped mode, current code

| metric | value |
|---|---|
| anchors (58): p50 / p95 / max | 0.000 / **0.098** / 0.423 m |
| dense lateral (318 det. frames): p50 / p95 | **0.023** / 0.341 m |
| naturalness violations | **0** |

vs the pre-campaign saved track: dense p95 0.483→0.341, anchors now all
within the joint-GT band, violations 0 both. In the product's designed
operating mode the ball sits at 0–10 cm at anchored moments, ~2 cm at
detected frames, with fully event-explained motion. The remaining
operational >20 cm tail (24 dense frames) is occlusion-adjacent
detection disagreement; the hold-out (auto) gap and per-clip levers are
logged above.

## Operational (as-shipped) closing table — 2026-08-18

| clip | anchors p95 / max | dense p50 / p95 | violations |
|---|---|---|---|
| gberch | **0.098** / 0.423 | **0.023** / 0.341 | **0** |
| origi01 | **0.103** / 0.437 | **0.049** / 0.902 | 9 |

W5j (rolls+carries naturalness-clean by construction): origi01 hold-out
violations 17→10, operational 9; the survivors are REAL bends without a
covering event (f405-class, 88°) — the validator's violation list is now
literally the worklist for the next lever: evidence-gated event minting
at uncovered bends (the spec's "violations become split proposals"),
then kroupi flight-span arc fits with endpoint refinement, occlusion
tails, and the 3 touch-lift anchor compromises per clip (~0.42-0.44 m).

## W5k — uncovered-bend event proposals (2026-08-18)

Violations on the resolved track become second-chance touch/bounce
candidates through ALL standard gates. origi01 operational: violations
9→8, dense p95 0.902→0.800, max 1.543→1.453; hold-out unchanged. The
surviving bends lack gate-clearable evidence at their peaks — they join
the occlusion-desert class. gberch operational regression check pending.

## W5l/W5m — flight arcs from detections; flip-on-fit coupling (2026-08-18)

- **W5l**: ballistic segments with no interior anchors fit their arcs from
  ≥3 in-span detection rays (manual endpoints near-hard, auto body-pins
  soft, smooth endpoint ramps). kroupi01 hold-out violations 4→0.
- **W5i re-trial with W5l**: flipping roll→ballistic on p_flight alone
  STILL regressed kroupi (fits never fired on its low-evidence spans) —
  reverted again.
- **W5m**: classification follows fit success — a roll span the IMM calls
  flight flips only when a detection arc ACCEPTS. kroupi keeps its best
  (2.52/0.567/0 violations); **gberch violations 8→2**, p50 0.271 (best),
  dense >20 cm frames 60→50; origi01 unchanged.

Hold-out naturalness is now ≈clean on gberch/kroupi; origi01's residual 9-10
bends are its distant-zoom jitter + uncovered-bend class.

## Current cross-clip table — W5m stack (hold-out, wasb)

| clip | p50 | p95 | max | violations | campaign-start p50 |
|---|---|---|---|---|---|
| gberch | **0.271** | 4.60 | 6.93 | 2 | 0.44 |
| origi01 | **0.480** | 4.93 | 24.2 | 10 | 0.90 |
| s013 | **0.516** | 3.93 | 6.61 | 1 | 4.90 |
| kroupi01 | 2.52 | 9.06 | 10.9 | 0 | 2.32 |

Dense lateral p50: 3.2–5.7 cm on all clips (s013 dense >20 cm 45→36).
Hold-out naturalness across all four clips: **13 violations total**
(gberch 2, s013 1, kroupi 0, origi01 10); operational gberch 0 / origi01 8.
Remaining >20 cm classes (all named, measured): occlusion deserts,
deep-flight monocular depth (f282 24 m), sparse-anchor information limit
(kroupi), touch-lift ±0.43 m GT-convention compromises, origi01
distant-zoom jitter bends.

## W5n — span-scaled chord gate (2026-08-18)

origi01 dense >20 cm 82→80, p95 4.93→4.70, p50 unchanged; gberch
byte-identical (false-cluster guard verified). Kept.

## Frontier note (end of this iteration stretch)

Solver-side marginal returns have flattened: the last several levers move
single frames, not distributions. The residual classes are evidence-
bounded — the highest-value next campaign is **detector fine-tune v2**
using weak labels regenerated from the NOW much-better solved tracks
(the v1 corpus builder exists: scripts/build_finetune_corpus.py), which
attacks occlusion deserts, kroupi's coverage (0.48), and origi01's jitter
at the source. Second: sub-frame cross-replay sync to make A2 a true
20 cm gate. Third: operator round-trip (the auto sidecars + violation
worklists now surface exactly where one anchor buys the most).

## Detector fine-tune v2 — measured, not adopted (2026-08-18)

Chain ran end-to-end: real outputs REGENERATED with campaign code (the
shipped sidecars are now current), corpus v2 built from the improved
tracks (holdout kroupi01), 30 epochs from stock init (val hit 0.82,
holdout ~0.32; run3). Stage-level: gberch trades coverage for precision
(dense n 337→264 but p95 2.67→0.79, violations 2→0; p50 0.271→0.346);
kroupi finds more evidence agreeing less (dense p50 0.567→1.33,
violations 0→5). Pre-committed swap criteria NOT met → default stays v1;
v2 kept at pretrained_weights/wasb_soccer_finetuned_v2.pth.tar for
study. Lesson: self-distillation from weak tracks shifts the
precision/recall balance, it does not mint new true coverage — coverage
gains need new gold data (more clips/operator labels), the documented
next evidence-level investment.

## SHIPPED STATE — regenerated outputs, all four clips (2026-08-18)

Graded directly on the as-shipped sidecars (campaign code, v1 detector,
operator anchors — the product's operating mode):

| clip | anchors p95 / max | dense p50 / p95 | dense >20 cm | violations |
|---|---|---|---|---|
| gberch | 0.098 / 0.423 | 0.023 / 0.341 | 23/318 | **0** |
| origi01 | 0.103 / 0.437 | 0.050 / 0.787 | 60/356 | 9 |
| kroupi01 | 0.203 / 0.420 | **0.061** / 2.63 | 12/46 | **0** |
| s013 | 0.175 / **0.204** | 0.040 / **0.229** | 13/172 | 6 |

88 % of all measurable frames sit inside 20 cm (892 graded); anchors 94 %
inside (all exceptions ≤ 0.44 m touch-lift class). kroupi01's shipped
dense median fell to 6.1 cm with the campaign stack + its 12 anchors —
the operator-anchored operating mode transforms even the weak-detection
clip. s013 is at the bar's edge everywhere measurable.

## W5o — state-aware suppression: tried, measured, REVERTED (2026-08-19)

Letting auto touches survive near non-touch manual anchors halved s013's
operational violations (6→3) but broke the hardest invariant on origi01:
a kept-anchor error jumped 0.44→2.61 m (an adjacent surviving auto
keyframe corrupted resolution near a manual moment). Operator moments are
sacred — full revert. (The bundled dense-GT spike-cleaning was separately
verified a no-op: the stage's IMM acceptance already filters teleports,
so the 108/892 >20 cm dense frames are REAL divergence, not grading
noise.) Open question for a fresh session: the exact mechanism by which
the adjacent auto keyframe displaced a manual-anchored frame's resolution
— worth a targeted probe before any retry.

## Touch-fate map (gberch shipped autos, 2026-08-19)

Per-manual-touch outcome: 4 STRICT (f49, f159, f310, f371); 1 WRONG-BONE
(f209: P003 l_foot vs r_foot — same player, foot confusion); 3 MISSED
(f56 knee, f192, f343) — all three with 3-4 hard detections within ±2:
the moments are evidenced but no event fired within tolerance (break
detection or gates dropped them). The strict-recall gap is therefore NOT
an evidence problem: 1 bone disambiguation + 3 investigable event-firing
misses. W5p (arc reparameterization): violations 14→6 across clips but
constant-speed timing cost 23 dense frames — replaced by a linear-speed
(quadratic-arc) schedule preserving deceleration physics (W5p2).

## W5p3 — standing render configuration (2026-08-19)

Smoothing → targeted linear-speed reparameterization (only spans still
violating after smoothing; clean spans keep raw evidence timing).
Operational, all four clips:

| clip | dense p50 / p95 | >20 cm | violations |
|---|---|---|---|
| gberch | 0.022 / 0.240 | 17/312 | **0** |
| origi01 | 0.049 / 0.881 | 65/354 | 6 |
| s013 | 0.041 / 0.229 | 13/172 | 5 |
| kroupi01 | 0.049 / 2.63 | 12/46 | **0** |

Violations 14→11 vs pre-W5p with dense at par. Two clips fully
violation-free operationally; the 11 survivors are 7 heading_breaks
(real uncovered bends — the W5k gate-resisting class) + 4 roll_speedups
on origi/s013 spans that resist legal smoothing. Next investigations
(fresh context): the 3 evidenced-but-unfired touch events (f56/192/343
class), same-player foot disambiguation, knot-level dense tails.

## W5q — joint-speed attribution term: tried, A/B-measured, REVERTED (2026-08-19)

Controlled A/B (same fold, same caches): KIN-ON strict 3/8 vs KIN-OFF
4/8 — raw FK joint speed at the contact moment carries jitter that
mislabels more than the swing signal fixes. The "missed touches" from the
fate map were suppression artifacts (events fired at/near every manual
touch; manual anchors correctly suppressed the duplicate autos), so the
entire remaining touch gap is same-player bone labels — needing a
cleaner-than-FK-speed signal (pixel-space ViTPose foot speed, or
contact-pose classification) — documented for future work. Relaxed
recall 5/8 both arms: the drift from w5e's 6/8 came from the W5f–W5p
stack, an accepted trade against its violation/tail gains.

## Segment-fit facts (instrumented, 2026-08-19)

kroupi01: 6/6 attempted fits REJECTED — residuals 9.7 / 19.9 / 26 / 76 /
302 / 349 px (|v0| up to 97 m/s): NOT a gate-tuning problem. Those spans
hide interior events (multiple bounces/touches between its 12 sparse
anchors) that a single arc cannot span. origi01 accepts 12/19 fits.
DIAGNOSIS UPGRADE: kroupi's 2.63 m dense p95 is "split-fit
implementable", not evidence-bound — the next build is two-arc split
fitting on rejection (free shared split point at the max-residual obs,
both medians gated; `solve_piecewise`'s split-and-retry already models
this). Diag surface (`segment_fits`, `roll_evidence`, rejected-residual
logs) is now permanent, so the next session starts from facts.

## W5r — split fitting built; kroupi verdict completed (2026-08-19)

Two-arc split fitting landed (tested: recovers a hidden interior bounce
to ≤0.35 m from detections alone; rejects junk). On kroupi's four ≥6-obs
rejected spans the halves ALSO decline — with single-arc medians of
26/302/349/76 px and an implied 97 m/s launch, those spans' observations
are chaotic (the fly-over false detections the flight gate keeps from
MINTING still pollute the fit stream). Triple-confirmed verdict: kroupi's
2.63 m dense p95 is bound by detection quality on that clip, exactly the
evidence frontier the detector-v2 experiment mapped. The split machinery
stays: it fires wherever a clean two-event span appears (all clips,
future footage), and declines correctly on chaos.

## W5s — operator truth in the observation stream (2026-08-19)

Detections contradicted by bracketing clicks (interpolated path, ≤6-frame
gaps, >60 px) are now vetoed BEFORE the IMM/events/fits consume them —
the same rule the grading already applied, extended to the solver.
gberch dense max 3.11→2.01 m (the f52-58 static lock neutralized);
kroupi unchanged (no close brackets around its chaos spans). Anchors,
violations, medians all unchanged — pure worst-case improvement.

## End-of-cycle position

18 kept fixes, 5 archived experiments, 77 commits. Every implementable
solver-side idea in the residual map has now been either built and kept
or built, measured, and archived with its lesson. The remaining 12 % of
>20 cm frames are triple-verified information limits of THIS data; the
three actions that move them are data-side and specified: (1) operator
anchors at the ~15 flagged spans (violation worklists + underconstrained
diags point exactly there), (2) new gold labels → detector v3 (corpus
builder ready; v2 proved self-distillation insufficient), (3) sub-frame
cross-replay sync (fixes then gate A2 at 20 cm). Each verifies same-day
via the harness.

## W5t — attempt-bar 0.5: measured neutral, reverted (2026-08-19)

Lowering the flight-fit attempt bar produced zero accepted flips on any
clip (origi's ground-rendered "flight-roll" hypothesis disproven: either
its IMM posterior sits below 0.5 there or the fits decline on residual).
Kept-fix count stands at 18; archived experiments at 6. This closes the
solver hypothesis space: every idea in the residual map has now been
built-and-kept or built-measured-archived. The strict goal's remaining
12 % is data-bound — proven from six independent directions (single-arc
impossibility, split-fit declines, detector self-distillation neutrality,
obs-chaos magnitudes, attempt-bar neutrality, and the operator-veto's
bounded reach). The three data-side actions in the end-of-cycle position
are the complete and only path to closing it.

## Cross-replay absolute-truth thread (W5u–W5z2, 2026-08-19/20)

The "data-side" sync item proved to be an algorithm problem — and solving
it produced the campaign's first absolute ground truth and its largest
single accuracy gain:

- **W5u**: robust offset estimator (click-vetoed conf≥0.6 obs, trimmed
  best-half objective) nails origi01↔origi02 sync at **−140.25 ±0.25 fr**
  (5.7 cm trimmed miss); the old −142/−145.75 were 1.75–5.5 fr off on
  dirty medians. Fix sensitivity across the minimum: p50 8.8 cm.
- Fixes validated against operator clicks at matched frames: 0.04–0.61 m
  off-ray → genuine sub-20 cm absolute GT at 60–94 m depths.
- **W5v**: motion gate (static furniture triangulates at any offset).
- **W5w/x**: epistemic hierarchy completed — clicks ray-hard, ALL
  monocular depths (buckets AND body-pins) soft, fixes depth-hard.
- **W5y/z/z2**: closed-form fix-arcs (LM stalls on near-parallel-ray
  landscapes), range-limited to their covered interval, split variant for
  deflected runs, click-gate calibrated to the fixes' measured noise.

**Measured effect on origi01 (absolute A2):** p50 3.15 → 1.07 m,
**p95 65.7 → 12.7 m, max 67.2 → 46.2 m**; chains [201,282], [282,310],
[390,411] accepted on fix arcs at 3–16 px click residuals. The 20–67 m
deep-flight errors — the campaign's worst verified mass — are collapsing
onto truth. NEXT WIRING: inject accepted fix-arcs into overlapping
segment hints so the dense render (not just the keyframes) follows the
arc; then the same for [338,363] (needs fixes there: only sparse).

## W5aa + thread closing (2026-08-20)

Accepted fix-arcs now own their overlapping ballistic segments — keyframes
and dense render trace one identical absolute-truth curve. Thread total on
origi01 absolute A2: **p50 3.15 → 1.05 m, p95 65.7 → 12.7 m, max 67 → 46 m**.
The fixes inside accepted arc ranges grade at ~0.1–0.3 m; the remaining
multi-metre A2 mass lies exactly in flight spans WITHOUT fix coverage
(origi02 overlaps only part of the clip) plus [338,363] (sparse fixes).
Scaling this further is a coverage question: more replay pairs per event
(the machinery now exploits them automatically — sync, motion gate,
closed-form arcs, split arcs, segment ownership are all in place).

## FINAL CERTIFICATION — branch tip, 91 commits (2026-08-20)

Suite: 727 passed, 1 known pre-existing failure (aerial-arc, documented on
main). Operational grades, current shipped outputs:

| clip | anchors p95/max | dense p50/p95 | dense >20 cm | violations |
|---|---|---|---|---|
| gberch | 0.098/0.423 | 0.022/0.240 | 17/312 | 0 |
| origi01 | 0.103/0.437 | 0.058/0.913 | 77/355 | 6 |
| kroupi01 | 0.203/0.420 | 0.061/2.631 | 12/46 | 0 |
| s013 | 0.175/0.204 | 0.040/0.229 | 13/172 | 6 |

origi01's dense drifted 66→77 through the fix-arc work: at 90 m depth the
absolute arcs out-rank individual detections whose own noise (±4–6 px ≈
20–30 cm) exceeds the bar — its ABSOLUTE metric (A2 vs fixes) improved
p50 3.15→1.05 / p95 65.7→12.7 in the same change set. Grading truth
against noisier evidence is the documented limit of the dense metric at
extreme depth.

Campaign totals: 24 kept root-cause fixes, 6 measured-and-archived
experiments, the eval harness + caches + diagnostic surfaces as permanent
infrastructure, and every residual >20 cm frame carrying a proof and a
priced continuation: replay coverage (automatic exploitation now built),
detector gold labels, or one operator anchor at a flagged span.

## 2026-08-20 — Replay-pair continuation attempted autonomously: s013 group g02 (W6)

s013's sync group has SIX gameplay replay partners (s008, s009, s011, s012,
s014, s015 — 0.81–1.0 alignment confidence, all clips on disk). The one
continuation path executable without new operator data: solve partner
cameras landmark-free, let the cross-replay machinery mint fixes.

**Result: 6/6 partner cameras globally wrong; new physics gate rejects all
junk; zero fixes minted; s013 baseline preserved.**

- s012 experiment: landmark-free solve "succeeded" (3–6 px anchor
  residuals), offset refined −131 → −127.75, 29 fixes at 17.5° median
  parallax — and 15/29 sat 6–8 m BELOW THE PITCH. Error is purely
  along-ray (perp ≤ 0.5 m): a globally wrong partner camera displaces
  fixes along the reference view ray, invisible to every lateral gate.
- Gate added (947eb13): `filter_physical_fixes` (court volume
  x[-15,120] y[-15,83] z[-0.05,30]) + `pair_geometry_trusted` (>20%
  impossible → whole pair rejected; impossible fixes share cameras+offset
  so survivors are the same error field inside the court). Rejection
  reason persisted in cross_replay diag.
- Multi-partner sweep: s008/s009/s011 solved landmark-free, s014/s015
  produced no track. Every triangulated pair 100% impossible → all
  rejected. Interlocking: s008's pairs with THREE partners all underground
  → s008's camera itself junk (pre-distortion residual 2.3e8 px).
- Good-pair regression: origi01↔origi02 fixes are 2/84 (2.4%) and 2/31
  (6.5%) impossible → trusted; only isolated z≈−0.3 noise fixes dropped.
- Verified end-to-end twice (overlay2 + sweep): pair rejected → no fixes
  file → s013 anchors grade byte-identical to certified baseline
  (p95 0.175 m, max 0.204 m). Dense-row deltas observed in overlays are
  observation-regeneration environment artifacts (proven by overlay1 ==
  overlay2 with and without junk fixes), not fix effects.

**Lesson (permanent):** operator-anchored cameras put fixes ON click rays
(0.04–0.61 m); landmark-free cameras put them underground. Cross-replay
absolute truth REQUIRES operator-reviewed partner cameras — the auto
camera path mints per-frame-plausible but globally-wrong solves on tight
replay angles. The machinery + gate now exploit any reviewed pair safely
and reject any wild one automatically.

**s013 replay continuation is data-blocked pending ~30 min of operator
anchor-editor review** on any one of s008/s009/s011/s012 (auto-anchor
starting points exist). Campaign state otherwise unchanged: 93 commits,
strict bar residual 119/885 dense frames, all solver-side hypotheses
built-or-archived.

## 2026-08-20 — Depth-aware noise floor: probed, MOOT

Certification listed one measurement-side refinement: a depth-aware dense
noise floor (hypothesis: at long range, detection pixel noise alone
exceeds 20 cm, so some dense failures would be unmeasurable rather than
wrong). Probed on real cameras: meters-per-pixel is 1.6/1.9/1.4 cm/px
median (2.1/2.3/1.5 p95) on origi01/s013/kroupi01 — a 2 px noise floor is
3–5 cm. Zero of the 102 >20cm dense failures are sub-floor; none has a
floor above 20 cm. The failures are real track-vs-ray disagreements, not
measurement noise. No metric change warranted; hypothesis closed.

All autonomous axes now exhausted and measured: solver-side (certified),
replay-pair data (W6 — needs operator camera review), measurement-side
(this probe). Remaining continuations are operator-gated: g02 partner
anchor review (~30 min), detector gold labels, anchors at flagged spans.

## 2026-08-20 — W7: the residual was NOT all data-bound. Off-ray touch keyframes fixed.

Re-examining the biggest failing span (origi01 [91–115]×24) instead of
trusting the flagged-span priors found a real solver bug with a 3-stage
mechanism:

1. Ground-touch keyframe: FK joint projects underground on the click ray
   → W2a fallback kept joint-depth (x,y), lifted vertically to z=r —
   laterally OFF the operator's click ray (1.2–1.7 m at 4 origi01
   anchors: f7/f111/f140/f155).
2. C4 ray-faithfulness pass: saw the off-ray reprojection, snapped back
   onto the ray at the meaningless inherited depth → ball rendered
   UNDERGROUND (f111 z=−1.49) and state overwritten to "flight".
3. Both adjacent roll segments dragged off the smooth detection path
   (which rolls exactly through ray∩ground — verified per-frame).

**Fix (2 commits):** ground-touch resolution snaps to ray∩z=r whenever
the ray elevation ≥ 3° (below the floor the grazing-ray vertical lift
survives — W2a's real defense); anchor_gt_world mirrors the physics
(no underground GT). Plus a self-guard on the quadratic-arc
reparameterization after it manufactured a speed ramp on gberch
(constant 0.55 m/frame → 0.24→0.78): the retiming is kept only when it
strictly lowers the span's violation count.

**Re-certification (same-day A/B, hot caches, 881 measurable frames):**

| clip | dense >20cm | dense p95 | viol |
|---|---|---|---|
| origi01 | 66 → 18 | 0.879 → 0.198 | 6 → 5 |
| gberch | 18 → 17 | 0.259 → 0.242 | 0 → 1 |
| kroupi01 | 12 → 12 | 2.606 | 0 |
| s013 | 13 → 13 | 0.229 | 3 |
| **TOTAL** | **109 → 60 (6.8%)** | | **9 → 9** |

origi01's four failing touch spans cleared entirely; its dense p95 is
now UNDER the 20 cm bar. Anchor tails (0.33–0.44 m) zeroed on all clips.
gberch carries one honest residual violation (post-smoothing kink at the
corrected keyframe; guard rightly refused a worse retiming).

**Lesson:** "proven data-bound" was over-claimed — the proof trusted the
flagged-span analysis without per-frame decomposition of WHY each span
missed its rays. One afternoon of looking at a single span's evidence
path found 48 frames of solver bug. The remaining 60 failing frames now
need the same per-span treatment before any data-bound claim.

## 2026-08-20 — W8: kroupi bucket-depth spike + remaining-span taxonomy

Per-span decomposition of the post-W7 residual (60 frames):

- **Junk detections graded as truth (~25 frames)**: goal-mouth clusters
  where detections teleport ±2.5 m frame-to-frame (gberch [395–401],
  s013 [202–211]) or march linearly off-screen to x=−494 at conf 0.7–0.9
  (s013 [133–143] — perfect −120 px/frame steps: extrapolation artifacts
  labeled `detector`). No physical ball moves like that; the dense
  metric needs a physics filter (queued).
- **Marginal 0.2 m carries (~10)**: s013 [148–151] dribble follows at
  0.18–0.23 m — threshold-straddling, honest.
- **kroupi bucket spike (fixed, W8)**: chain [118,126] honestly rejects
  (unanchored lift), fallback kept airborne_low z=1.0 → 5.9 m deep on
  shallow rays → 19.2 m out-and-back at 65 m/s peak between touches,
  invisible to both metrics (ungraded rows + event-window exemption).
  Fix: rejected-chain fallback redepths a lone bucket anchor from the
  gravity arc through its hard neighbours, projected onto its click ray.
  Path 19.2→7.7 m, peak 65→27 m/s, p95 2.606→2.256; also fixed a 22 m
  airborne_mid displacement at f130. +1 marginal graded frame; other
  clips byte-identical.
- **origi01 [429–431]**: short-chip arc disagrees with a real flying
  detection path (5 frames, segment-fit span too short) — smallest class.

Metric gaps noted for the next pass: the naturalness event-window
exemption hid a 65 m/s spike; dense grades extrapolation junk as truth.

## 2026-08-20 — W9: flight-contradicted veto (junk detections relabeled)

Ballistic spans never consume interior detections, so the rendered arc
judges them non-circularly. Hard detections far off the arc (>60 px) that
are teleport islands (≤2 frames unreachable at 40 m/s) or static lock-ons
(≤1.5 px/frame while the arc moves) are relabeled `flight_veto` in the
observations sidecar after the solve — tracks byte-identical, grading and
downstream consumers stop treating them as truth.

**4-clip A/B (W8→W9): failing 61→56 of 876 (6.4%).** kroupi p95
2.256→1.432 (post-goal teleport islands gone), s013 0.229→0.218, origi01
0.198→0.186. gberch unchanged — its goal-mouth oscillation straddles the
60 px far threshold and the thresholds are physics constants, not knobs.

Session totals (certified 13.4% → now 6.4%): W7 off-ray touches −48,
W7b reparam guard, W8 bucket redepth (quality, +1), W9 junk relabel −5.
Remaining 56: gberch oscillators straddling far-px (~6) + [182-187],
s013 drifting hover (4.3 px/frame — deliberately kept, ambiguous) +
marginal carries, origi01 [369-374] bobble + [426-434] chip, scattered
singletons. Each class named; next levers: operator anchors (cover all),
detector v3 at flagged spans, or per-class solver work (chip-span fits
below the 3-obs minimum; bobble = possible micro-bounce modeling).

## 2026-08-20 — W10: veto boundary-connectivity fix; MIN_OBS no-op

W9's island rule had a false-veto bug: origi01's REAL chip flight
(430–431) was killed as a "teleport island" because the vetoable window
excluded the boundary detection it feasibly connects to. Fixed:
components reachable at ball speed from span-boundary evidence are never
islands. 3 falsely-vetoed rows restored, all failing honestly:
**56→59 of 879 (6.7%)** — a worse number that is more true.
_SEGMENT_FIT_MIN_OBS 3→2 measured as a corpus-wide no-op (every
candidate span already had ≥3 interior obs); reverted.

**Day total: certified 119/885 (13.4%) → 59/879 (6.7%).** Remaining 59:
goal-mouth oscillators straddling the far threshold (gberch ~6+),
ambiguous drifting hover + marginal carries (s013), post-goal chaos
within shot-speed budget (kroupi), bobble/chip spans + singletons
(origi01). Every threshold is a physics constant; every class has a
named lever (operator anchors cover all; detector gold labels the rest).

## 2026-08-21 — W11: chip span proven DATA-contradictory; gate stays 6

Lowering the ballistic span floor to 4 let origi01's chip [429,433]
attempt its fit for the first time — and the rejection is the finding:
gravity through its two operator anchors (4 frames apart) allows ~3 cm
of apex, while the restored interior detections climb ~130 px. No
gravity arc can reach them: either those anchors or those detections
are wrong. Reclassified from "solver-reachable" to operator review;
floor reverted (corpus no-op otherwise). Trimmed-retry idea also
closed: the median gate already tolerates one junk obs in three
(pinned by new tests). Residual stands at 59/879 (6.7%); the two
"unbuilt solver ideas" are now one (micro-bounce modeling for
origi01 [369-374]).

## 2026-08-21 — W12 (final, goal stopped by user): hop span also data-contradictory

Median-robust flight candidacy (median pf 0.86 vs mean 0.78 at origi01
[366,375]) opened the fit attempt the mean gate refused — and the fit
honestly rejected, split too: the detection rays demand ~0.5–0.9 m
column heights no gravity arc through the bracketing GROUND anchors can
reach (max apex ~0.16 m over 9 frames). Same verdict as the chip
(W11): the span's anchors and detections are mutually contradictory —
operator review, not solver work. Change was corpus-neutral and
reverted per discipline; the finding is the deliverable.

## CAMPAIGN CLOSE (user-directed stop, 2026-08-21)

Certified start: 119/885 dense frames >20 cm (13.4%).
**Final: 59/879 (6.7%)** — anchors 0 >20 cm on all four clips, dense
p95 origi01 0.198 / gberch 0.242 / s013 0.218 / kroupi01 2.022,
violations 9 (net unchanged), suite 687 green + 1 documented
pre-existing failure. ~110 commits on ball-sub20cm-accuracy.

Shipped this campaign-close arc: W6 replay-fix physics gate, W7 ground
touches on the click ray (−48), W7b reparam self-guard, W8 bucket
redepth from neighbours, W9 flight-contradicted veto, W10 veto
boundary-connectivity integrity (+3 honest), W11/W12 negative results
reclassifying the chip and hop spans as data-contradictory.

Every remaining failing frame is classified; none is solver-reachable
under the campaign's own measurement discipline. Levers per class in
the operator handoff (2026-08-20-operator-handoff.md): anchors at
contradictory/junk spans, partner-camera review for s013 replay fixes,
detector gold labels.
